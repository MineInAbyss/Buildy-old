rootProject.name = 'buildy'

if (useLocalGuiy.toBoolean()) includeBuild '../guiy'

if (useLocalIdofront.toBoolean()) includeBuild '../idofront'

if (useLocalKotlinSpice.toBoolean()) includeBuild('../kotlinspice')