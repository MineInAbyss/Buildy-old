rootProject.name = 'Buildy'

if (useLocalGuiy.toBoolean()) {
    include 'guiy'
    project(':guiy').projectDir = new File('../guiy')
}

if (useLocalIdofront.toBoolean()) {
    include 'idofront'
    project(':idofront').projectDir = new File('../idofront')
}